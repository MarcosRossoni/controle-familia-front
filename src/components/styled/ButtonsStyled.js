import styled from "styled-components";

let heightTela = (0.425 * window.screen.height)
export const Speeddial = styled.div`
    position: relative;
    min-height: ${heightTela}px;
    margin: 0 10px 0 0;
`